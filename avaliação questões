ESTRUTURAS DE DECISÃO:
/* 1) Faça um algoritmo que leia os valores A, B, C e imprima na tela se a soma de A + B é menor
que C. 
    let a = parseFloat(prompt("Digite um numero a"))
    let b = parseFloat(prompt("Digite um numero b"))
    let c = parseFloat(prompt("Digite um numero c"))
    if (a + b < c) {
        alert("A+B é menor que C")
    } else if (a + b > c) {
        alert("A+B é maior que C")
    }
    else {
        alert("A+B é igual a C")
    }
    */
 
 
 /*2) Faça um algoritmo que leia o nome, o sexo e o estado civil de uma pessoa. Caso sexo seja “F” e
estado civil seja “CASADA”, solicitar o tempo de casada (anos). 
    let nome = prompt("Digite seu nome")
    let sexo = prompt("Qual seu sexo ?(M/F)")
    let estadoCivil =prompt("Qual seu estado civil?(C/S)")

    if (sexo == "F" && estadoCivil == "C" ){
        let tempo = prompt("Quanto tempo de casado?")
        alert(tempo)
    }
    */
   
   ESTRUTURA DE REPETIÇÃO :

    /* 1) Desenvolver um algoritmo que efetue a soma de todos os números ímpares que são múltiplos de
 três e que se encontram no conjunto dos números de 1 até 500.
 
 let soma = 0
 for (let i = 0; i < 500; i++) {
     // verificação se e impar
      if( i % 2 != 0)
      // verificação se e divisivel por 3
        if( i % 3 == 0)
        soma = soma + i
   console.log(soma);
 }*/

/*6) Escrever um algoritmo que gera e escreve os números ímpares entre 100 e 200
     
    
      for(let a = 100; a <=200; a++)
        if(a % 3 == 0){
            console.log(a)
        }*/
        
 
 
 
